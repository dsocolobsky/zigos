pub const TSS = packed struct(u104) {
    reserved0: u16,
    rsp0: u32,
    rsp1: u32,
    rsp2: u32,
    reserved1: u32,
    ist1: u32,
    ist2: u32,
    ist3: u32,
    ist4: u32,
    ist5: u32,
    ist6: u32,
    ist7: u32,
    reserved2: u40,
    iomap_base: u8,
};
